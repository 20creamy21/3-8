 <html>
   <body>

     <p><%= flash[:notice] %></p>

     <h1>Books</h1>

     <table>
       <thead>
         <tr>
           <th>Title</th>
           <th>Body</th>
           <th colspan　= "3"></th>
         </tr>
       </thead>

       <tbody>
        <% @books.each do |list| %>
         <tr>
          <td><%= list.title %></td>
          <td><%= list.body %></td>
          <td><%= link_to "Show", book_path(list.id) %></td>
          <td><%= link_to "Edit", edit_book_path(list.id) %></td>
          <td><%= link_to "Destroy", "/books/#{list.id}", method: :delete %></td>
         </tr>
        <% end %>
       </tbody>
     </table>

     <%= form_with model:@book, local:true do |f| %>
      <h2>New book</h2>
      <h6>Title</h6>
       <div><%= f.text_field :title %></div>
      <h6>Body</h6>
       <div><%= f.text_area :body %></div>
      <div><%= f.submit "Create Book" %></div>
     <% end %>

   </body>
 </html>